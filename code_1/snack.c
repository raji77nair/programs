#include<stdio.h>
int price(int x);
int main()
{
    int total;
    printf("total: %d",total);
}
int product(int x)
{
    int X,Y;
    printf("enter the code 1:");
    scanf("%d",x);
    printf("enter the code 2:");
    scanf("%d",Y);

x=y;
        float price = 0;

        if (X == 1)
         {

            price  = (float) (4.00 * Y);

        }
        else if (X == 2) 
        {

            price  = (float) (4.50 * Y);

        }
        else if (X == 3) 
        {

            price  = (float) (5.00 * Y);

        }
        else if (X == 4) 
        {

            price  = (float) (2.00 * Y);

        }
        else if (X == 5) 
        {

            price  = (float) (1.50 * Y);

        }
        total=X+y;
        return total;
}